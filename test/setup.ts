// Vitest setup — wires @testing-library/jest-dom matchers (toBeInTheDocument,
// toHaveClass, etc.) into expect(). Imported automatically via the
// `setupFiles` entry in vitest.config.ts before each test file.
import '@testing-library/jest-dom/vitest';
