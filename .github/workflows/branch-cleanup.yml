# Löscht erledigte Remote-Branches über scripts/cleanup-merged-branches.sh.
# Manuell auszulösen (Actions → Branch Cleanup → Run workflow).
# Ohne gesetztes "apply" läuft nur der Trockenlauf.
name: Branch Cleanup

on:
  workflow_dispatch:
    inputs:
      apply:
        description: "Branches wirklich löschen (sonst Trockenlauf)"
        type: boolean
        default: false

permissions:
  contents: write
  pull-requests: read

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      # Voller Commit-SHA statt `@v4`: Ein beweglicher Tag laesst sich vom
      # Action-Repo umhaengen — das ist der Lieferketten-Angriff, gegen den
      # `test/ci/actions-pinned.test.ts` steht. Derselbe SHA wie in den
      # uebrigen 25 Verwendungen dieses Repos.
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0
      - name: Cleanup ausführen
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          chmod +x scripts/cleanup-merged-branches.sh
          if [ "${{ inputs.apply }}" = "true" ]; then
            ./scripts/cleanup-merged-branches.sh --apply
          else
            ./scripts/cleanup-merged-branches.sh
          fi
