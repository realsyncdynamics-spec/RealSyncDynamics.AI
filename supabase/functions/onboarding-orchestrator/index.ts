import 'jsr:@supabase/functions-js/edge-runtime.d.ts';
import { createClient } from 'jsr:@supabase/supabase-js@2';
const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type','Access-Control-Allow-Methods':'POST, OPTIONS'};
const ok=(b:unknown,s=200)=>new Response(JSON.stringify(b),{status:s,headers:{...cors,'content-type':'application/json'}});
const err=(s:number,c:string,m:string)=>ok({ok:false,error:{code:c,message:m}},s);
const sectors=new Set(['saas','agency','healthcare','public_sector','generic']);
function domainOf(raw:string|null){if(!raw)return null;try{return new URL(raw.includes('://')?raw:`https://${raw}`).hostname.toLowerCase().replace(/^www\./,'')}catch{return null}}
function industryOf(s:string){return ({saas:'software',agency:'professional-services',healthcare:'healthcare',public_sector:'public-sector',generic:'other'} as Record<string,string>)[s]??'other'}
function persona(s:string,c:string){const r=({saas:'Produkt, Vertrieb, Support und Operations',agency:'Kundenkommunikation, Projekte, Vertrieb und Delivery',healthcare:'administrative Prozesse; keine Diagnose oder individuelle medizinische Beratung',public_sector:'Verwaltungs- und Organisationsprozesse; rechtswirksame Entscheidungen bleiben beim Menschen',generic:'Geschäftsprozesse und Kundenkommunikation'} as Record<string,string>)[s];return `Du bist der digitale Unternehmensassistent von ${c}. Du unterstützt ${r}. Arbeite nachvollziehbar, datensparsam und eskaliere risikoreiche Entscheidungen an Menschen.`}
async function one(admin:any,table:string,match:Record<string,unknown>,values:Record<string,unknown>){let q=admin.from(table).select('*').limit(1);for(const [k,v] of Object.entries(match))q=q.eq(k,v);const {data,error}=await q.maybeSingle();if(error)throw error;if(data){const {data:u,error:e}=await admin.from(table).update(values).eq('id',data.id).select('*').single();if(e)throw e;return {row:u,created:false}}const {data:i,error:e}=await admin.from(table).insert({...match,...values}).select('*').single();if(e)throw e;return {row:i,created:true}}
Deno.serve(async req=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});if(req.method!=='POST')return err(405,'METHOD_NOT_ALLOWED','method not allowed');const h=req.headers.get('Authorization');if(!h?.startsWith('Bearer '))return err(401,'UNAUTHORIZED','Authorization header required');let body:any;try{body=await req.json()}catch{return err(400,'BAD_REQUEST','invalid json')}if(!sectors.has(body?.sector))return err(400,'INVALID_SECTOR','Invalid sector value');const url=Deno.env.get('SUPABASE_URL'),key=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY');if(!url||!key)return err(500,'INTERNAL','Supabase configuration missing');const admin=createClient(url,key,{auth:{persistSession:false}});const {data:{user},error:ae}=await admin.auth.getUser(h.replace(/^Bearer\s+/i,''));if(ae||!user)return err(401,'UNAUTHORIZED','Invalid token');try{let tenantId=body.tenantId??null;if(tenantId){const {data:m}=await admin.from('memberships').select('id').eq('tenant_id',tenantId).eq('user_id',user.id).maybeSingle();if(!m)return err(403,'FORBIDDEN','not a member of this tenant')}else{const {data:ms,error:e}=await admin.from('memberships').select('tenant_id').eq('user_id',user.id).limit(2);if(e)throw e;if(!ms?.length)return err(400,'TENANT_NOT_FOUND','No tenant found for this user');if(ms.length>1)return err(400,'TENANT_AMBIGUOUS','Multiple tenants — tenantId required');tenantId=ms[0].tenant_id}
const a=body.answers&&typeof body.answers==='object'&&!Array.isArray(body.answers)?body.answers:{};const company=String(a.companyName??a.company_name??a.company??a.orgName??a.org_name??'Unternehmen').trim()||'Unternehmen';const websiteRaw=typeof a.website==='string'?a.website.trim():typeof a.websiteUrl==='string'?a.websiteUrl.trim():null;const domain=domainOf(websiteRaw);const sector=body.sector;const industry=industryOf(sector);const now=new Date().toISOString();await admin.from('tenants').update({company_name:company,org_name:company,industry,onboarded_at:now,is_public_sector:sector==='public_sector'}).eq('id',tenantId);const profile=await one(admin,'company_profiles',{tenant_id:tenantId},{sector,onboarding_answers:a,updated_at:now});let website:any=null;if(domain)website=(await one(admin,'websites',{tenant_id:tenantId,domain},{status:'audit_pending',plan_tier:'audit',updated_at:now,notes:`Provisioned during onboarding from ${websiteRaw}`})).row;const ais=await one(admin,'ai_systems',{tenant_id:tenantId,name:`${company} AI Assistant`},{vendor:'RealSyncDynamics.AI',model_name:'managed',department:'operations',owner_email:user.email??null,purpose:`AI assistant for ${company}; sector=${sector}`,data_types:['business_data','customer_communications'],ai_act_class:'unknown',risk_score:0,status:'active',updated_at:now});const existingBot=await one(admin,'bots',{tenant_id:tenantId,name:`${company} Assistant`},{description:`Automatically provisioned business assistant for ${company}`,channel:'chat',persona:persona(sector,company),greeting:`Hallo! Ich bin der digitale Assistent von ${company}. Wie kann ich helfen?`,capabilities:{chat:true,voice:true,governance:true,appointments:true},config:{ai_system_id:ais.row.id,sector,residency:'inherit'},enabled:true,updated_at:now});let agentId=(existingBot.row.config??{}).agent_profile_id??null;if(!agentId){const ap=await admin.from('agent_profiles').insert({name:`${company} Assistant`,type:'business',description:`Self-service AI agent for ${company}`,system_prompt:persona(sector,company),enabled:true}).select('*').single();if(ap.error)throw ap.error;agentId=ap.data.id;const cfg={...(existingBot.row.config??{}),agent_profile_id:agentId,ai_system_id:ais.row.id,sector,residency:'inherit'};const {error:e}=await admin.from('bots').update({config:cfg}).eq('id',existingBot.row.id);if(e)throw e}const cfg=await one(admin,'agent_configuration',{tenant_id:tenantId},{enabled:true,monthly_token_budget:100000,hourly_rate_limit:120,cache_enabled:true,cache_ttl_minutes:15,fallback_to_rules:true,auto_generate_digests:true,auto_generate_insights:true,alert_on_budget_warning:true,warning_threshold_percent:80,updated_at:now});const kb=await one(admin,'agent_knowledge_base',{agent_id:agentId,tenant_id:tenantId,title:'Onboarding company context'},{source_type:'onboarding',content:JSON.stringify({company,sector,industry,website:websiteRaw,domain,answers:a}),tags:['onboarding',sector,'company-context']});const packs=['dsgvo-essentials','iso-27001-foundation'];if(sector==='healthcare')packs.push('eu-ai-act-high-risk');if(sector==='public_sector')packs.push('nis2-cybersecurity');if(sector==='generic'){}for(const pack_id of packs)await admin.from('policy_pack_activations').upsert({tenant_id:tenantId,pack_id,activated_at:now,created_by:user.id},{onConflict:'tenant_id,pack_id',ignoreDuplicates:true});await admin.from('inventory_audit_events').insert({tenant_id:tenantId,actor_user_id:user.id,action:'ONBOARDING_PROVISIONED',target_type:'tenant',target_id:tenantId,new_value:{sector,company,domain,ai_system_id:ais.row.id,bot_id:existingBot.row.id,agent_profile_id:agentId},reason:'Automated onboarding provisioning',source:'onboarding-orchestrator',occurred_at:now});await admin.from('enterprise_ai_audit_events').insert({tenant_id:tenantId,actor:user.email??user.id,action:'ONBOARDING_PROVISIONED',system_name:`${company} AI Assistant`,risk_level:'pending',metadata:{sector,ai_system_id:ais.row.id,bot_id:existingBot.row.id,agent_profile_id:agentId,website_id:website?.id??null},created_at:now});return ok({ok:true,success:true,tenant_id:tenantId,provisioned:{company_profile_id:profile.row.id,website_id:website?.id??null,ai_system_id:ais.row.id,bot_id:existingBot.row.id,agent_profile_id:agentId,agent_configuration_id:cfg.row.id,knowledge_base_id:kb.row.id,policy_packs:packs},next:{chat:'bot-chat',voice:'bot-voice-webhook',governance:['ai-act-risk-inventory','governance-agent','governance-risk-score']}})}catch(e){console.error(e);return err(500,'PROVISIONING_FAILED','Automatic onboarding provisioning failed')}})
