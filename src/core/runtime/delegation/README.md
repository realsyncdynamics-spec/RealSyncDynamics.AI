# Delegation Graph

Arbeitsbranch `feat/delegation-graph` only. Kein Merge nach main. Kein Deploy.

Primitive sitzt in `src/core/runtime/delegation/`.
Executor faltet die Kette **vor** `permissions.check`.
GOVARD bleibt Decision Boundary.
