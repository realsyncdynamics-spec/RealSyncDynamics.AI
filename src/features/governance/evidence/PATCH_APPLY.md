# Apply locally (branch already has panel + tests)

```bash
git checkout feat/evidence-chain-os-integrity
git checkout main -- src/features/governance/evidence/EvidenceVaultView.tsx
```

Then apply the four hunks from the conversation (import, TabId, TABS row, render).

Do not merge. Do not deploy.
