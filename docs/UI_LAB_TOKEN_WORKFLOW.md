# UI Lab — Token-Export und Impact-Analyse

- Quelle: `src/index.css` und `src/styles/context-themes.css`.
- `command-center-density.css` bleibt Legacy-Binding für `var(--dash-*)`.
- Drafts speichern nur Diffs.
- Kein Landing-/SiteOS-Touch.
- Scope: `--dash-title-size`, `--dash-title-size-sm`, `--dash-kpi-min-height`, `--context-radius-card`.
- `--dash-kpi-min-height` hält KPI-Karten synchron.
- Merge nur ohne `blocked` oder `unmapped`; Radius bleibt `0`.
