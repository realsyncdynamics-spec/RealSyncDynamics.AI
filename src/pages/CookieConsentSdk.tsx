import { Link } from 'react-router-dom';
import { ArrowRight } from 'lucide-react';
import { COMPETITOR_PRICING } from '../config/competitor-pricing';

// Pricing note: competitor value is centralized in COMPETITOR_PRICING.
// Existing page content remains unchanged except for the comparison copy below.
