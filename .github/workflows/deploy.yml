  functions-deploy:
    name: Deploy edge functions
    runs-on: ubuntu-latest
    # Decoupled from db-push so a failing migration-repair step can't block
    # the function deploy. Functions never depend on a successful migration
    # at deploy time — they validate schema at runtime.
    if: github.event_name == 'push' || inputs.deploy_functions == true
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      # supabase link is required so the CLI can resolve the project context
      # when reading per-function flags from supabase/config.toml.
      - name: Link project
        run: supabase link --project-ref "${{ secrets.SUPABASE_PROJECT_ID }}"

      # Deploy strategy:
      # `supabase functions deploy` alone has historically been unreliable
      # at applying per-function verify_jwt settings from supabase/config.toml
      # on remote projects (config.toml is local-dev-first; remote-flag
      # propagation depends on CLI version + project context). To
      # guarantee correctness, parse config.toml and deploy each function
      # individually with the explicit --no-verify-jwt flag where needed.
      - name: Deploy edge functions (per-function, explicit JWT flag)
        run: |
          set -e
          # Extract names of functions with verify_jwt = false from config.toml.
          NO_JWT_FUNCS=$(awk '
            /^\[functions\./ { name = $0; sub(/^\[functions\./, "", name); sub(/\]$/, "", name); next }
            /^verify_jwt[[:space:]]*=[[:space:]]*false/ && name != "" { print name; name = "" }
            /^\[/ && !/^\[functions\./ { name = "" }
          ' supabase/config.toml | sort -u)

          echo "::group::Functions with verify_jwt=false"
          echo "$NO_JWT_FUNCS"
          echo "::endgroup::"

          # Deploy each function under supabase/functions/* with the correct flag.
          # _shared, _internal, .ts/.md files are skipped (directories only).
          for dir in supabase/functions/*/; do
            fn=$(basename "$dir")
            case "$fn" in
              _*) continue ;;
            esac
            if [ ! -f "$dir/index.ts" ]; then continue; fi

            if echo "$NO_JWT_FUNCS" | grep -qx "$fn"; then
              echo "→ deploy $fn --no-verify-jwt"
              supabase functions deploy "$fn" --no-verify-jwt --project-ref "${{ secrets.SUPABASE_PROJECT_ID }}"
            else
              echo "→ deploy $fn (verify_jwt=default)"
              supabase functions deploy "$fn" --project-ref "${{ secrets.SUPABASE_PROJECT_ID }}"
            fi
          done

      # Smoke-verify that public functions actually deployed with verify_jwt=false.
      # If this fails the workflow surface shows the regression instead of
      # leaving prod in a silent 401 state.
      #
      # IMPORTANT: invalid payloads only. The request must pass the JWT
      # gateway (would be 401 if verify_jwt=true) and fail validation
      # *before* the function performs any DB inserts — that keeps the
      # smoke check non-mutating and rate-limit-free across many deploys.
      - name: Verify public endpoints (non-mutating)
        run: |
          set -e
          # gdpr-audit: invalid URL → 400 INVALID_URL before any DB write.
          gdpr_status=$(curl -s -o /dev/null -w "%{http_code}" \
            -X POST "https://${{ secrets.SUPABASE_PROJECT_ID }}.supabase.co/functions/v1/gdpr-audit" \
            -H 'content-type: application/json' \
            -d '{"url":"not-a-url","email":"smoke@example.invalid"}' --max-time 15)
          # cookie-scan: invalid URL → 400 before any persistence.
          cookie_status=$(curl -s -o /dev/null -w "%{http_code}" \
            -X POST "https://${{ secrets.SUPABASE_PROJECT_ID }}.supabase.co/functions/v1/cookie-scan" \
            -H 'content-type: application/json' \
            -d '{"url":"not-a-url"}' --max-time 15)
          echo "gdpr-audit no-auth: $gdpr_status"
          echo "cookie-scan no-auth: $cookie_status"
          # 400 (validation rejected) = function reachable, verify_jwt=false applied.
          # 401 = gateway rejection = config didn't apply.
          if [ "$gdpr_status" = "401" ] || [ "$cookie_status" = "401" ]; then
            echo "::error::Public function returned 401 after deploy — verify_jwt config didn't apply"
            exit 1
          fi
