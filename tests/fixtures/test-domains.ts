/**
 * Test-Domains für Audit-Tests.
 * 'safe' ist eine Domain, die für automatisierte Scans freigegeben ist.
 * Niemals echte Kundendaten verwenden.
 */
export const testDomains = {
  safe: 'example.com',
  withTrackers: 'httpbin.org',
  invalid: 'not-a-real-domain-xyz123.invalid',
};
